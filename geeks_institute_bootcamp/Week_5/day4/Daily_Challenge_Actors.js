/*1.SELECT COUNT(*) FROM actors;
2.Si les colonnes sont en NOT NULL, l’insertion échoue avec une erreur.
Sinon, la ligne sera insérée avec des valeurs NULL, ce qui n’est généralement pas souhaitable.
*/